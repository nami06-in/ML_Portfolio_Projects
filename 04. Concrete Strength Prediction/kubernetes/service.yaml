apiVersion: v1
kind: Service

metadata:
  name: concrete-strength-service

spec:
  type: NodePort

  selector:
    app: concrete-strength-app

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007